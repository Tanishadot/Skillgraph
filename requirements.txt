# ── API server ────────────────────────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.29.0
python-multipart>=0.0.9
pydantic>=2.6.0

# ── ML pipeline ───────────────────────────────────────────────────────────────
torch>=2.1.0
sentence-transformers>=3.1.0,<4.0
transformers>=4.38.0,<5.0
numpy>=1.24.0
scikit-learn>=1.3.0
rank-bm25>=0.2.2

# ── Utilities ─────────────────────────────────────────────────────────────────
tqdm>=4.65.0
pyyaml>=6.0.1

# ── Optional: AI Copilot (set ANTHROPIC_API_KEY env var to enable) ────────────
anthropic>=0.30.0

# ── Optional: presentation artifacts only (pitch deck + architecture diagram) ─
# python-pptx>=1.0.0
# matplotlib>=3.8.0
